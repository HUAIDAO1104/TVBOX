# 全局功能与交互修复报告

本次更新版本：**5.5.65-search-experience，versionCode 616**。合并前两轮搜索问题和全局体验审查，重点处理操作响应、等待时间、结果完整性、页面连续性和错误恢复。

## 搜索结果为何数量不一致

原来的来源数字与网格使用了不同口径：来源侧统计原始返回条目，网格还会过滤无关内容、预告和花絮，并按来源条目 ID 去重；第一页也不等于来源全部结果。另外，来源使用名称子串匹配，名称相近或配置名称重复时可能误归类。

现在来源数字直接对应实际可显示卡片；标题栏的来源数按不同站点计算。保存来源选择改用稳定 ID，旧选择先做名称精确匹配再迁移，不会把“光影”自动扩展到“光影影视”。不再因为来源名字而附加没有视频质量依据的“4K”标签。

## 已实施的修复

| 范围 | 更新后的行为 |
|---|---|
| 切换来源 | 遥控器焦点移到来源即切换，点击确认仍可用；快速移动只采用最后停留位置 |
| 来源数字 | 相关性过滤、去重和网格使用同一份结果；空来源显示 0，状态区分排队、搜索中、失败、超时、停止 |
| 大量结果 | 标题匹配、分组、跨源候选、海报索引在独立后台任务执行；非追加列表差异计算也移至后台；切源直接显示缓存列表 |
| 慢源隔离 | 原生来源由两个私有进程执行，各槽在整个应用内串行；22 秒看门狗回收无响应进程，取消也能终止忽略中断的插件 |
| HTTP 搜索 | 与原生来源分开调度，最多三个并发请求；取消会取消 HTTP Call；标题先返回，补图失败保留已有条目 |
| 优先顺序 | 查看中的来源优先排队；健康状态和有效返回优先于首页来源身份；空返回不提高健康排名 |
| 重试与分页 | “搜索操作”中可以仅重试当前源、失败/停止源，或重新搜索全部；当前源可请求下一页；已显示结果保留，页数和重复条目限制后续分页 |
| 空来源导航 | 可直接查看全部已返回结果，不必清空重搜 |
| 返回与重建 | 同一 ViewModel 会话复用；最近 3 次搜索保留 2 分钟缓存，单次超过 4000 条不进入缓存；配置、来源选择、账号变更隔离缓存 |
| 浏览位置 | 每个来源保存自己的滚动位置和卡片锚点；切回时恢复，左侧焦点不被拉进网格 |
| 手动换源 | 卡片来源面板使用跨源候选索引；其他站点的同作品可直接打开；徽标显示不同站点数 |
| 搜索输入 | 250 ms 防抖、旧请求取消、最近建议缓存和生命周期校验；手机恢复系统输入法 |
| 分类筛选 | 220 ms 合并连续筛选；加载期间保留旧内容，成功后替换，失败保留；空分页终止继续加载 |
| 首页启动 | 热启动不再强制等待最短品牌动画，确认键可以跳过；启动工作不再人为延后 1.4 秒 |
| 首页简介 | 独立低优先级执行器和 12 秒任务预算；离开首页取消；后台原生搜索/详情使用隔离进程，缓存条目有容量上限 |
| 直播切台 | 地址请求使用前台专用队列；120 ms 合并连续切台；新地址就绪前保留旧画面；迟到地址不能覆盖新选择 |
| 点播换源 | 自动恢复显示阶段和次数，并加入 45 秒总等待预算；取消时同时取消待处理详情/播放请求，迟到结果不重新启动播放 |
| 弹幕搜索 | 累积排序移到后台，用请求编号和版本丢弃旧结果；新结果不抢走输入框焦点 |
| 观看历史 | 返回页面重新读取；清空需确认；首页重复长按只切换删除模式，不再清空全部历史 |
| 收藏 | 列表和配置读取异步化；打开过程防重复，并显示配置切换提示；取消后旧回调不能打开其他收藏 |
| 仓库管理 | 列表、条目统计和映射统计后台读取；映射数量使用单次 SQL；合并刷新，列表使用稳定 ID 和差异更新；详情刷新立即显示同步状态 |
| 网盘与配置 | 发现/登录界面有 30 秒超时和回调代次检查；配置加载有总时限、超时反馈和旧回调隔离 |
| 海报与播放缓存 | 海报索引最多 512 个作品、每作品 12 个候选；重复标题归一化缓存；播放缓存预热，预加载等缓存就绪后启动 |
| 维护操作 | 清缓存通过播放器缓存 API，不直接删除其持有目录；保留运行中的来源组件；清理/备份防重复并反馈状态；备份写临时文件，完整压缩同步后再替换正式备份 |
| 缓存设置说明 | 容量上限明确提示下次启动应用生效；预加载时长和线程数提示下次播放生效 |

## 验证与性能记录

固定数据压力测试使用 API 36 Android TV arm64 模拟器，不请求真实搜索源。数据集分别为 500、1000、3000 条；所有卡片完整保留。下表是一轮采样，主线程响应是测试线程向主线程投递空任务并等待的延迟，**不是帧率、用户按键 P95，也不是网络搜索时间**。

| 条目数 | 后台处理与列表交付总耗时 | 主线程投递延迟 P95 | 最大延迟 |
|---:|---:|---:|---:|
| 500 | 324 ms | 29 ms | 29 ms |
| 1000 | 506 ms | 1 ms | 13 ms |
| 3000 | 1509 ms | 1 ms | 10 ms |

另一轮采样中，3000 条交付为 1585 ms，主线程最大延迟 64 ms。最终回归与宿主 R8 打包同时执行时，500 / 1000 / 3000 条分别为 1003 / 1687 / 4297 ms，主线程投递最大延迟分别为 124 / 55 / 116 ms；负载竞争会明显影响结果。样本较少，只能说明此固定场景下大量标题处理不再持续占用主线程，不能外推为所有电视、所有来源的固定加速倍数。原先 800 条同步处理耗时对照保留在 `PERFORMANCE_BENCHMARK_2026-09-06.json`，其统计口径与新后台交付不同。

独立进程故障测试包括两种情况：HTTP 请求不响应，以及实际加载的测试 Jar 故意无限等待并忽略中断。看门狗能够回收进程；取消后和超时后的下一次请求可以返回结果；应用主进程没有随之退出。测试 Jar 只进入测试 APK，源码与再生成脚本一并保留。

搜索专项同时覆盖焦点切换、数量一致性、快照替换、首批不额外等待、慢补图、分页、取消和异常恢复。扩展回归检查搜索/播放布局、播放入口、600 集列表、历史入口和弹幕设置。旧测试中的非主线程 View 创建、未测量布局尺寸断言已改为有效的主线程布局验证。

最终测试汇总、签名、四种正式安装包及 OTA 校验结果见 `UX_RELEASE_CHECKLIST_2026-09-06.md`。旧审查报告记录的是当时状态，本报告描述本次实现。

## 验证范围

已做代码回归、固定数据压力测试、故障夹具测试与正式包启动 / 真实源抽测。正式 TV 64 位包以“庆余年”查询 5 个默认来源时，返回 9 条可显示卡片（至臻 2、虎斑 7），玩偶 0，木偶和热播未成功。木偶插件的 `DexNative.getSpider` 在工作进程触发 JNI 崩溃，主界面存活、其他来源继续返回；该第三方插件在此模拟器上的兼容性未解决，不能宣称所有来源均可用。没有把真实仓库所有第三方来源、所有网盘登录协议、各播放器内核或用户电视上的网络耗时视为已验证。真实源本身的延迟、限流、分页支持和资源失效仍会影响搜索完成时间；现在这些情况有独立状态、超时回收和增量重试入口。

正式版包含电视/手机、32/64 位四种安装包，沿用既有签名和 OTA 资产命名。

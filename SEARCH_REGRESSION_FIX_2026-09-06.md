# 5.5.66 搜索性能回退修复

版本：`5.5.66-search-hotfix` / `617`。针对用户反馈的进入搜索卡顿、少量来源搜索仍很慢及大量超时，重新检查 5.5.65 的实际请求链路。本次问题包含上一版引入的回退，不能归结为来源自身速度。

## 原因与修改

| 原因 | 修改 |
|---|---|
| 每次搜索完成也销毁进程，下一源重新加载插件；运行时缓存全部失效 | 成功请求保留并复用工作进程及插件实例；同源优先使用已有实例；空闲 60 秒释放，异常/取消仍能回收 |
| 默认只有两个串行槽，慢源阻挡后续来源 | 普通设备最多四个独立工作进程；低内存设备或物理内存不足 1.5 GiB 时最多两个；每进程仍只执行一个原生搜索 |
| 缺少 MD5 的 HTTP Jar 在每个新进程重新下载，各进程还有重复文件 | 共享下载缓存；跨进程文件锁避免重复下载，临时文件完成后验证并原子替换；有 MD5 时必须一致；无 MD5 时验证 Dex 包结构，缓存六小时 |
| 普通 Jar 也预先初始化 Python 和 QuickJS | 按实际来源类型延迟初始化对应运行环境 |
| 独立进程没有启动插件依赖的本地代理，端口未初始化 | 在执行前启动工作进程自己的回环代理，支持该进程中的插件代理、文件和内存凭证读取；不开放远程界面操作 |
| 初始化失败被永久缓存成空插件，后续查询一直空结果 | 失败的插件实例不进入缓存；工作进程向界面报告失败，并允许重试重新初始化 |
| 部分封装插件的初始化返回空 ClassLoader，随后将空对象传入 JNI 导致崩溃 | 检查插件公开的 loader 就绪接口；未就绪只做两次短重试，仍失败则返回初始化错误，不继续进入 JNI |
| 工作进程崩溃被统一标为超时 | 根据实际等待时间区分立即运行失败与看门狗超时 |
| 来源列表逐行扫描全仓库并多次计算 SHA-256 / 格式化，主线程承担平方级工作 | 批量生成来源名称和 ID 映射；来源选择一次建立索引；移除逐字节 String.format，保持已有 ID 不变 |

焦点即切源、结果数字与网格统一口径、分页、增量重试、已有结果保留及后台索引继续保留。没有通过少搜来源或减少显示条数来制造提速。

## 对照公开实现

未取得并核验用户所用影视仓具体版本的完整源码，因此不声称已经复刻其内部实现。对照了可核验的 [TVBox SourceViewModel](https://github.com/takagen99/Box/blob/main/app/src/main/java/com/github/tvbox/osc/viewmodel/SourceViewModel.java) 与 [ApiConfig](https://github.com/takagen99/Box/blob/main/app/src/main/java/com/github/tvbox/osc/api/ApiConfig.java)：搜索使用并行请求、按源交付，并通过加载器获取插件。此次重点修复重复初始化和重复下载，同时保留原生异常隔离，避免重新引入主进程崩溃。

## 真实来源对照

同一 API 36 Android TV arm64 模拟器、同一个 98 源配置、只选择同样五个来源；请求真实接口，未用模拟数据替代网络结果。两次关键词不同，因此第二次不能命中同关键词的结果缓存。仪器测试采用正常页面跳转，保留首页，避免 ActivityScenario 清理任务栈导致配置被首页销毁回调清空。

旧版 5.5.65 记录：

| 关键词 | 启动调用至仪器空闲 | 首批有效结果回调 | 全部五源结束 | 可显示结果 |
|---|---:|---:|---:|---:|
| 庆余年 | 1144 ms | 6314 ms | 12812 ms | 19 |
| 庆余年第二季 | 1149 ms | 7643 ms | 10519 ms | 1 |

这些是网络抽样值，服务返回会变化。不能把不同时间的条目数量差异视为固定数据丢失，也不能把启动调用至仪器空闲等同于首帧时间。以下是最终代码的复测记录。

## 验证范围

固定数据回归覆盖切源、数量一致性、快速焦点移动、分页、首批交付、500/1000/3000 条处理、慢补图及异常恢复。新增真实测试 Jar 验证：连续查询保留相同进程和同一个插件实例；并发进程仅下载一次 Jar；错误 MD5 被拒绝且不破坏旧缓存；初始化第一次未就绪时，在构造插件前完成就绪校验；插件能在搜索中访问自身本地代理。继续测试无限等待且忽略中断的插件被回收后，后续请求仍能运行。

未将模拟器抽测写成所有电视和第三方来源都固定“秒出”的保证。重点是移除应用自身重复工作和错误环境初始化，保留逐源进度、失败区分和取消能力。

## 5.5.66 最终实测

| 关键词 | 系统记录结果页首帧 | 启动调用至仪器空闲 | 首批有效结果回调 | 网格首次绘制结果 | 全部五源结束 | 可显示结果 |
|---|---:|---:|---:|---:|---:|---:|
| 庆余年 | 183 ms | 464 ms | 2032 ms | 2114 ms | 4379 ms | 13 |
| 庆余年第二季 | 160 ms | 622 ms | 656 ms | 669 ms | 1751 ms | 3 |

与旧版同口径相比，首批结果从 6314 / 7643 ms 降至 2032 / 656 ms，五源全部结束从 12812 / 10519 ms 降至 4379 / 1751 ms。两轮均无超时；第一轮 3 个源成功、2 个失败，第二轮 2 个成功、2 个空结果、1 个失败。崩溃缓冲区为空。旧版没有记录网格首次绘制时间，因此不做这一指标的前后对比。

随后覆盖安装实际经过 R8 压缩的 TV arm64 正式 APK，从首页打开搜索页（系统首帧 80 ms），选择历史关键词进入结果页（系统首帧 116 ms）。真实接口返回 8 条结果、2 个成功来源；按遥控器向下键，焦点从“全部”移至“玩偶”，内容立即变为该源的 6 条；再次向下移至“至臻”，立即显示 2 条与侧栏数字一致的结果，全程未按确认键。

正式包这一轮另有 3 个来源失败。日志仍可见第三方封装插件内部读取空 InputStream 的初始化异常；现在会作为失败返回，不进入空 ClassLoader 的 JNI 调用、不挂住其他来源。第三方二进制内部逻辑和接口可用性尚不能保证，不能将本次修复表述成“所有来源均正常”。本次没有修改该第三方二进制。

## 完成的检查与交付

- JVM 单元测试：249 项，0 失败、0 错误。
- API 36 Android TV 设备回归：28 项通过，包含进程复用、下载合并、代理、初始化就绪、强制回收、分页及遥控器操作。
- 真实来源测量：独立 opt-in 测试通过；运行期间不并行构建，避免构建争抢资源影响结果。原始指标保存在 `SEARCH_LATENCY_2026-09-06.json`。
- Android Lint：0 错误；290 项警告仍存在，本次没有将它们表述为全部解决。
- TV / 手机的 armeabi-v7a、arm64-v8a 共四种正式 APK 构建成功，均为版本 617 / 5.5.66-search-hotfix，签名与旧版一致，V1 / V2 / V3 校验通过；正式包不包含测试来源。
- 四个正式安装包和 OTA 清单作为 v5.5.66 交付；适配真实电视芯片和用户网络仍需实际使用反馈，本文测量环境为模拟器。
